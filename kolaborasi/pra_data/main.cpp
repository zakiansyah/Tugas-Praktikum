#include <iostream>
#include <fstream>
#include "library/input.h"
#include "library/proses.h"
#include "library/output.h"
using namespace std;

int main(){
  Input input;
  input.getdata;
  input.toFile;
  
  Proses proses;
  proses.getdata;
  proses.tofile;
  
  Output output;
  output.getData;
  output.cetak;
}
